# Geliştirme Asistanı Raporu

- **Görev:** ASTRO V1 ROS 2 Humble + Arduino Mega Faz 1 Yazılım Mimarisi: Jetson Orin Nano ile Arduino Mega arasında 115200/500000 bps USB Serial haberleşme. Arduino'da: BTS7960 motor sürücüsü (Sol/Sağ PWM kontrol), TMC2209 stepper (Kafa rotasyonu UART modu), dual enkoder (Sol/Sağ tekerlek dış kesme), Luxonis OAK-D-Lite-AF IMU I2C okuma (6-axis accel+gyro), CRC-8 korumalı seri paket yapısı, heartbeat+watchdog 500ms timeout, motor PID kontrolü 50 Hz. ROS 2 Humble'da: astro_base, astro_description, astro_bringup paketleri. ROS Topics: /joint_states, /wheel_cmds, /head_cmd, /imu/data_raw, /arduino/diagnostics. PlatformIO Arduino standardı, colcon build. Hardware+Software watchdog. Seri paket parser CRC-8 kontrolü.
- **Tarih:** 2026-06-10 19:29:31
- **Mühendis modeli (GPT):** `LLMName.OPENAI_GPT5`
- **İnceleyici modeli (Claude):** `LLMName.CLAUDE_V4_5_SONNET`
- **Test/QA modeli (Gemini):** `LLMName.GEMINI_2_5_PRO`
- **Kaydedilen kod dosyaları:** generated_20260610_192429_1.txt, generated_20260610_192429_2.txt, generated_20260610_192429_3.txt, generated_20260610_192429_4.txt, generated_20260610_192429_5.txt, generated_20260610_192429_6.txt, generated_20260610_192429_7.txt, generated_20260610_192429_8.txt, generated_20260610_192429_9.txt, generated_20260610_192429_10.txt, generated_20260610_192429_11.txt, generated_20260610_192429_12.txt, generated_20260610_192429_13.txt, generated_20260610_192429_14.txt, generated_20260610_192429_15.txt, generated_20260610_192429_16.txt, generated_20260610_192429_17.txt, generated_20260610_192429_18.txt

## AŞAMA 1 — GPT (Yazılım Mühendisi): Üretilen Kod

*Model: `LLMName.OPENAI_GPT5`*

Aşağıda, ASTRO V1 Faz-1 için ROS 2 Humble (Jetson Orin Nano) ↔ Arduino Mega mimarisini gerçekleştiren tam iskelet proje dosyaları verilmiştir. Kod; güvenilir bir CRC-8 korumalı seri paket yapısı, 500 ms heartbeat/watchdog, 50 Hz motor PID kontrol döngüsü, enkoder kesmeleri, TMC2209 kafa (UART ile konfigüre, step/dir sürüş), I2C IMU (6-DOF) okuma ve ROS 2 tarafında gerekli düğümler/mesajlar/launch dosyalarını içerir. Derleme: Arduino tarafı PlatformIO; ROS 2 tarafı colcon (ament_cmake + ament_cmake_python).  

Notlar:
- IMU: Örnek olarak MPU-6050 kullanıldı (I2C, 0x68). OAK-D-Lite üzerinde bir IMU varsa I2C pass-through (ve doğru adres/sürücü) ile uyarlayınız.
- BTS7960 sürücüsü: her motor için iki PWM hattı (fwd/rev) ile yön+PWM sürüş (Mega PWM pinleri kullanıldı).
- TMC2209: UART ile akım/mikrostep konfigürasyonu; hareket step/dir ile AccelStepper üzerinden.
- ROS mesjaları astro_base/msg altında tanımlı: WheelCmd.msg, HeadCmd.msg.
- Paket yapısı: astro_base (mesajlar + Python node), astro_description (URDF), astro_bringup (launch).

-------------------------------------------------------------------
Path: arduino/astro_firmware/platformio.ini
```ini
[env:megaatmega2560]
platform = atmelavr
board = megaatmega2560
framework = arduino

; Seri port hız ayarı test/çalışma sırasında değiştirilebilir
monitor_speed = 115200

lib_deps =
  Wire
  https://github.com/teemuatlut/TMCStepper.git
  https://github.com/waspinator/AccelStepper.git

build_flags =
  -O2
  -std=gnu++17
  -DARDUINO_USB_MODE=1

```

-------------------------------------------------------------------
Path: arduino/astro_firmware/include/pins.h
```cpp
#pragma once

// Motor sürücü BTS7960 pinleri (örnek):
// Her motor için iki PWM: FWD ve REV. Enable pinleri opsiyonel.
#define L_MOTOR_PWM_FWD   5
#define L_MOTOR_PWM_REV   6
#define R_MOTOR_PWM_FWD   7
#define R_MOTOR_PWM_REV   8

// Enkoder pinleri (Mega dış kesme destekli A kanalları: 2,3,18,19,20,21)
#define L_ENC_A 2     // INT0
#define L_ENC_B 4
#define R_ENC_A 3     // INT1
#define R_ENC_B 9

// TMC2209 Step/Dir pinleri (kafa):
#define HEAD_STEP_PIN  10
#define HEAD_DIR_PIN   11
#define HEAD_EN_PIN    12

// TMC2209 UART (Mega üzerinde HW Serial1 kullanımı önerilir: TX1=18, RX1=19)
#define TMC2209_SERIAL Serial1
#define TMC2209_ADDRESS 0b00  // MS1/MS2 adres pinlerine göre ayarlayın

// IMU I2C adresi (MPU-6050 için tipik adres)
#define IMU_I2C_ADDR 0x68

// LED / Durum
#define STATUS_LED 13
```

-------------------------------------------------------------------
Path: arduino/astro_firmware/include/protocol.h
```cpp
#pragma once
#include <Arduino.h>

// Paket Yapısı:
// [0xAA][0x55][LEN][MSG_ID][PAYLOAD...][CRC8]
// CRC8 polinom: 0x07, init 0x00 (CRC-8-ATM)
namespace Proto {
  static const uint8_t SOF1 = 0xAA;
  static const uint8_t SOF2 = 0x55;

  enum MsgId : uint8_t {
    HEARTBEAT        = 0x01,  // host->mcu
    WHEEL_CMD        = 0x02,  // host->mcu: float32 left_rpm, right_rpm
    HEAD_CMD         = 0x03,  // host->mcu: float32 angle_deg
    IMU_DATA         = 0x10,  // mcu->host: 6x float32 (ax,ay,az,gx,gy,gz), uint32 micros
    ENCODER_TICKS    = 0x11,  // mcu->host: int32 L, int32 R, uint32 dt_us
    DIAGNOSTICS      = 0x12,  // mcu->host: uint16 vbat_mV, int16 mcu_temp_cX100, uint32 flags
    HEARTBEAT_ACK    = 0x13   // mcu->host
  };

  inline uint8_t crc8(const uint8_t* data, size_t len) {
    uint8_t crc = 0x00;
    for (size_t i = 0; i < len; ++i) {
      crc ^= data[i];
      for (uint8_t b = 0; b < 8; ++b) {
        if (crc & 0x80) crc = (crc << 1) ^ 0x07;
        else crc <<= 1;
      }
    }
    return crc;
  }

  inline void writePacket(Stream& s, uint8_t msg_id, const uint8_t* payload, uint8_t payload_len) {
    uint8_t len = 1 + payload_len; // msg_id + payload
    s.write(SOF1);
    s.write(SOF2);
    s.write(len);
    s.write(msg_id);
    if (payload_len > 0) s.write(payload, payload_len);

    uint8_t buf[1 + 255]; // length + data
    buf[0] = len;
    buf[1] = msg_id;
    memcpy(&buf[2], payload, payload_len);
    uint8_t crc = crc8(buf, 2 + payload_len);
    s.write(crc);
  }

  struct Parser {
    enum State { WAIT_SOF1, WAIT_SOF2, WAIT_LEN, WAIT_DATA, WAIT_CRC } state = WAIT_SOF1;
    uint8_t len = 0;
    uint8_t data[256];
    uint8_t idx = 0;

    bool feed(uint8_t b, uint8_t& out_id, const uint8_t*& out_payload, uint8_t& out_pl_len) {
      switch (state) {
        case WAIT_SOF1:
          if (b == SOF1) state = WAIT_SOF2;
          break;
        case WAIT_SOF2:
          if (b == SOF2) state = WAIT_LEN;
          else state = WAIT_SOF1;
          break;
        case WAIT_LEN:
          len = b;
          idx = 0;
          state = WAIT_DATA;
          break;
        case WAIT_DATA:
          data[idx++] = b;
          if (idx >= len) state = WAIT_CRC;
          break;
        case WAIT_CRC: {
          uint8_t crc = crc8(&len, 1); // start with len
          crc = crc8(data, len); // wrong; fix below
          // Fix: compute over [len][data...]
          uint8_t tmp[1 + 255];
          tmp[0] = len;
          memcpy(&tmp[1], data, len);
          uint8_t calc = crc8(tmp, 1 + len);
          if (calc == b) {
            out_id = data[0];
            out_payload = &data[1];
            out_pl_len = len - 1;
            state = WAIT_SOF1;
            return true;
          } else {
            // CRC fail -> reset parser
            state = WAIT_SOF1;
          }
        } break;
      }
      return false;
    }
  };
}
```

-------------------------------------------------------------------
Path: arduino/astro_firmware/src/main.cpp
```cpp
#include <Arduino.h>
#include <Wire.h>
#include <TMCStepper.h>
#include <AccelStepper.h>
#include <avr/wdt.h>

#include "pins.h"
#include "protocol.h"

// ====== Parametreler ======
static constexpr uint32_t SERIAL_BAUD = 500000; // 115200 alternatif
static constexpr float CONTROL_HZ = 50.0f;
static constexpr uint32_t CONTROL_DT_MS = (uint32_t)(1000.0f / CONTROL_HZ);

static constexpr int32_t TICKS_PER_REV_L = 2048; // enkoder CPR*4 uygun biçimde ayarlayın
static constexpr int32_t TICKS_PER_REV_R = 2048;

static constexpr float WHEEL_R_L = 0.06f; // metre (örnek 60mm)
static constexpr float WHEEL_R_R = 0.06f;

static constexpr float KP = 0.6f, KI = 0.2f, KD = 0.0f; // 50 Hz PID için örnek
static constexpr int PWM_MAX = 255;

// ====== Global Durum ======
volatile int32_t g_left_ticks = 0;
volatile int32_t g_right_ticks = 0;

static int32_t g_left_last_ticks = 0;
static int32_t g_right_last_ticks = 0;

static float g_left_target_rpm = 0.0f;
static float g_right_target_rpm = 0.0f;

static float g_left_err_i = 0.0f, g_right_err_i = 0.0f;
static float g_left_err_prev = 0.0f, g_right_err_prev = 0.0f;

static uint32_t g_last_control_ms = 0;
static uint32_t g_last_heartbeat_ms = 0;

static bool g_motors_enabled = true;

// IMU ham değerleri -> m/s2, rad/s dönüştürülecek
static float ax, ay, az, gx, gy, gz; 
static uint32_t imu_last_us = 0;

// TMC2209 (UART üzerinden konfigürasyon)
TMC2209Stepper tmc2209(&TMC2209_SERIAL, 0.11f, TMC2209_ADDRESS); // 0.11 ohm örnek Rsense
AccelStepper head_stepper(AccelStepper::DRIVER, HEAD_STEP_PIN, HEAD_DIR_PIN);

// ====== Yardımcılar ======
inline void setMotorPWM(int pwm_fwd_pin, int pwm_rev_pin, int val) {
  val = constrain(val, -PWM_MAX, PWM_MAX);
  if (val >= 0) {
    analogWrite(pwm_fwd_pin, val);
    analogWrite(pwm_rev_pin, 0);
  } else {
    analogWrite(pwm_fwd_pin, 0);
    analogWrite(pwm_rev_pin, -val);
  }
}

inline void setLeftPWM(int v) { setMotorPWM(L_MOTOR_PWM_FWD, L_MOTOR_PWM_REV, v); }
inline void setRightPWM(int v){ setMotorPWM(R_MOTOR_PWM_FWD, R_MOTOR_PWM_REV, v); }

void IRAM_ATTR leftEncA() {
  // Quadrature yön tespiti
  bool b = digitalRead(L_ENC_B);
  g_left_ticks += b ? -1 : +1;
}
void IRAM_ATTR rightEncA() {
  bool b = digitalRead(R_ENC_B);
  g_right_ticks += b ? -1 : +1;
}

void imuInit() {
  Wire.begin();
  delay(50);
  // MPU-6050: power management 0x6B = 0x00
  Wire.beginTransmission(IMU_I2C_ADDR);
  Wire.write(0x6B); Wire.write(0x00);
  Wire.endTransmission();

  // Gyro full-scale = ±2000 dps (0x1B=0x18), Accel ±2g (0x1C=0x00)
  Wire.beginTransmission(IMU_I2C_ADDR);
  Wire.write(0x1B); Wire.write(0x18);
  Wire.endTransmission();

  Wire.beginTransmission(IMU_I2C_ADDR);
  Wire.write(0x1C); Wire.write(0x00);
  Wire.endTransmission();
}

bool imuRead() {
  // Okuma: ACCEL_XOUT_H (0x3B) -> 14 byte
  Wire.beginTransmission(IMU_I2C_ADDR);
  Wire.write(0x3B);
  if (Wire.endTransmission(false) != 0) return false;

  uint8_t n = Wire.requestFrom(IMU_I2C_ADDR, 14u, true);
  if (n != 14) return false;

  auto rd = []() -> int16_t {
    int16_t h = Wire.read();
    int16_t l = Wire.read();
    return (int16_t)((h << 8) | l);
  };

  int16_t axr = rd();
  int16_t ayr = rd();
  int16_t azr = rd();
  int16_t tmp = rd(); (void)tmp;
  int16_t gxr = rd();
  int16_t gyr = rd();
  int16_t gzr = rd();

  // Dönüşümler
  // Accel: ±2g -> LSB/g = 16384, g=9.80665
  const float A_SCALE = 9.80665f / 16384.0f;
  // Gyro: ±2000 dps -> 16.4 LSB/(°/s); rad/s = dps * pi/180
  const float G_SCALE = (PI / 180.0f) / 16.4f;

  ax = axr * A_SCALE;
  ay = ayr * A_SCALE;
  az = azr * A_SCALE;
  gx = gxr * G_SCALE;
  gy = gyr * G_SCALE;
  gz = gzr * G_SCALE;

  imu_last_us = micros();
  return true;
}

void tmcInit() {
  pinMode(HEAD_EN_PIN, OUTPUT);
  digitalWrite(HEAD_EN_PIN, LOW); // enable low active olabilir, donanıma göre ayarlayın

  TMC2209_SERIAL.begin(115200);
  delay(50);
  tmc2209.begin();
  tmc2209.pdn_disable(true);
  tmc2209.I_scale_analog(false);
  tmc2209.rms_current(500); // mA
  tmc2209.microsteps(16);
  tmc2209.en_spreadCycle(false);
  tmc2209.TCOOLTHRS(0xFFFFF);

  head_stepper.setMaxSpeed(2000);     // steps/s
  head_stepper.setAcceleration(2000); // steps/s^2
}

void setupIO() {
  pinMode(STATUS_LED, OUTPUT);
  pinMode(L_MOTOR_PWM_FWD, OUTPUT);
  pinMode(L_MOTOR_PWM_REV, OUTPUT);
  pinMode(R_MOTOR_PWM_FWD, OUTPUT);
  pinMode(R_MOTOR_PWM_REV, OUTPUT);

  pinMode(L_ENC_A, INPUT_PULLUP);
  pinMode(L_ENC_B, INPUT_PULLUP);
  pinMode(R_ENC_A, INPUT_PULLUP);
  pinMode(R_ENC_B, INPUT_PULLUP);

  attachInterrupt(digitalPinToInterrupt(L_ENC_A), leftEncA, RISING);
  attachInterrupt(digitalPinToInterrupt(R_ENC_A), rightEncA, RISING);
}

void stopMotors() {
  setLeftPWM(0);
  setRightPWM(0);
}

void publishIMU() {
  uint8_t payload[6 * 4 + 4];
  // floatları little-endian olarak kopyala
  float vals[6] = {ax, ay, az, gx, gy, gz};
  memcpy(&payload[0], vals, sizeof(vals));
  memcpy(&payload[6 * 4], &imu_last_us, 4);
  Proto::writePacket(Serial, Proto::IMU_DATA, payload, sizeof(payload));
}

void publishEncoders(uint32_t dt_us, int32_t dl, int32_t dr) {
  uint8_t payload[4 + 4 + 4];
  memcpy(&payload[0], &dl, 4);
  memcpy(&payload[4], &dr, 4);
  memcpy(&payload[8], &dt_us, 4);
  Proto::writePacket(Serial, Proto::ENCODER_TICKS, payload, sizeof(payload));
}

void publishDiag(uint16_t vbat_mV, int16_t temp_cX100, uint32_t flags) {
  uint8_t payload[2 + 2 + 4];
  memcpy(&payload[0], &vbat_mV, 2);
  memcpy(&payload[2], &temp_cX100, 2);
  memcpy(&payload[4], &flags, 4);
  Proto::writePacket(Serial, Proto::DIAGNOSTICS, payload, sizeof(payload));
}

void loopControl() {
  uint32_t now = millis();
  if (now - g_last_control_ms < CONTROL_DT_MS) return;
  uint32_t dt_ms = now - g_last_control_ms;
  g_last_control_ms = now;

  // Watchdog: 500 ms içinde heartbeat/komut gelmezse motorları kes
  if (now - g_last_heartbeat_ms > 500) {
    g_motors_enabled = false;
    stopMotors();
  } else {
    g_motors_enabled = true;
  }

  // Hız hesapla (ticks -> rpm)
  int32_t l_ticks = g_left_ticks;
  int32_t r_ticks = g_right_ticks;
  int32_t dl = l_ticks - g_left_last_ticks;
  int32_t dr = r_ticks - g_right_last_ticks;
  g_left_last_ticks = l_ticks;
  g_right_last_ticks = r_ticks;

  float dt_min = dt_ms / 60000.0f; // ms -> dakika
  float l_rpm_meas = (dl / (float)TICKS_PER_REV_L) / dt_min;
  float r_rpm_meas = (dr / (float)TICKS_PER_REV_R) / dt_min;

  // PID
  auto pid_step = [&](float target_rpm, float meas_rpm, float& e_i, float& e_prev)->int {
    float e = target_rpm - meas_rpm;
    e_i += e * (dt_ms / 1000.0f);
    e_i = constrain(e_i, -200.0f, 200.0f); // anti-windup
    float de = (e - e_prev) / (dt_ms / 1000.0f);
    e_prev = e;
    float u = KP * e + KI * e_i + KD * de;
    int pwm = (int)constrain(u, -PWM_MAX, PWM_MAX);
    return pwm;
  };

  int l_pwm = 0, r_pwm = 0;
  if (g_motors_enabled) {
    l_pwm = pid_step(g_left_target_rpm, l_rpm_meas, g_left_err_i, g_left_err_prev);
    r_pwm = pid_step(g_right_target_rpm, r_rpm_meas, g_right_err_i, g_right_err_prev);
  }
  setLeftPWM(l_pwm);
  setRightPWM(r_pwm);

  // IMU oku (50 Hz)
  imuRead();
  publishIMU();
  publishEncoders(dt_ms * 1000u, dl, dr);

  // Basit diagnostik: bayraklar
  uint16_t vbat = 12000; // mV (örn. gelecekte ADC ile ölç)
  int16_t temp = 2500;   // 25.00 C
  uint32_t flags = 0;
  if (!g_motors_enabled) flags |= 0x01;
  publishDiag(vbat, temp, flags);
}

void processPacket(uint8_t msg_id, const uint8_t* pl, uint8_t len) {
  switch (msg_id) {
    case Proto::HEARTBEAT: {
      g_last_heartbeat_ms = millis();
      Proto::writePacket(Serial, Proto::HEARTBEAT_ACK, nullptr, 0);
      digitalWrite(STATUS_LED, !digitalRead(STATUS_LED));
    } break;
    case Proto::WHEEL_CMD: {
      if (len < 8) break;
      memcpy(&g_left_target_rpm, &pl[0], 4);
      memcpy(&g_right_target_rpm, &pl[4], 4);
      g_last_heartbeat_ms = millis(); // komut da heartbeat sayılır
    } break;
    case Proto::HEAD_CMD: {
      if (len < 4) break;
      float angle_deg;
      memcpy(&angle_deg, &pl[0], 4);
      // steps/degree hesabı: motor_steps_per_rev * microsteps / 360
      const float motor_steps_per_rev = 200.0f; // 1.8 derece stepper
      const float micro = 16.0f; // tmc ayarında
      float steps_per_deg = (motor_steps_per_rev * micro) / 360.0f;
      long target_steps = lroundf(angle_deg * steps_per_deg);
      head_stepper.moveTo(target_steps);
      g_last_heartbeat_ms = millis();
    } break;
  }
}

void setup() {
  // HW Watchdog: 1s
  wdt_enable(WDTO_1S);

  setupIO();
  Serial.begin(SERIAL_BAUD);
  imuInit();
  tmcInit();

  g_last_control_ms = millis();
  g_last_heartbeat_ms = millis();

  // PWM frekansları varsayılan; istenirse timer ayarları optimize edilebilir
  analogWrite(L_MOTOR_PWM_FWD, 0);
  analogWrite(L_MOTOR_PWM_REV, 0);
  analogWrite(R_MOTOR_PWM_FWD, 0);
  analogWrite(R_MOTOR_PWM_REV, 0);
}

void loop() {
  // Watchdog besle
  wdt_reset();

  // TMC/Stepper güncelle
  head_stepper.run();

  // Seri parser
  static Proto::Parser parser;
  while (Serial.available() > 0) {
    uint8_t b = Serial.read();
    uint8_t id; const uint8_t* payload; uint8_t pl_len;
    if (parser.feed(b, id, payload, pl_len)) {
      processPacket(id, payload, pl_len);
    }
  }

  // Kontrol döngüsü 50 Hz
  loopControl();
}
```

-------------------------------------------------------------------
Path: ros2_ws/src/astro_base/package.xml
```xml
<?xml version="1.0"?>
<package format="3">
  <name>astro_base</name>
  <version>0.1.0</version>
  <description>ASTRO V1 base ROS2 interfaces and serial bridge node.</description>
  <maintainer email="dev@example.com">ASTRO Dev</maintainer>
  <license>Apache-2.0</license>

  <buildtool_depend>ament_cmake</buildtool_depend>
  <buildtool_depend>ament_cmake_python</buildtool_depend>

  <build_depend>rosidl_default_generators</build_depend>
  <exec_depend>rosidl_default_runtime</exec_depend>

  <depend>rclpy</depend>
  <depend>std_msgs</depend>
  <depend>sensor_msgs</depend>
  <depend>diagnostic_msgs</depend>
  <depend>builtin_interfaces</depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
```

-------------------------------------------------------------------
Path: ros2_ws/src/astro_base/CMakeLists.txt
```cmake
cmake_minimum_required(VERSION 3.10)
project(astro_base)

find_package(ament_cmake REQUIRED)
find_package(ament_cmake_python REQUIRED)
find_package(rosidl_default_generators REQUIRED)

# Mesajlar
set(MSG_FILES
  "msg/WheelCmd.msg"
  "msg/HeadCmd.msg"
)
rosidl_generate_interfaces(${PROJECT_NAME}
  ${MSG_FILES}
  DEPENDENCIES builtin_interfaces
)

# Python paketini kur
ament_python_install_package(${PROJECT_NAME})

# Python node kurulumu
install(PROGRAMS
  src/serial_bridge.py
  DESTINATION lib/${PROJECT_NAME}
)

ament_export_dependencies(rosidl_default_runtime)
ament_package()
```

-------------------------------------------------------------------
Path: ros2_ws/src/astro_base/msg/WheelCmd.msg
```msg
float32 left_rpm
float32 right_rpm
```

-------------------------------------------------------------------
Path: ros2_ws/src/astro_base/msg/HeadCmd.msg
```msg
float32 angle_deg
```

-------------------------------------------------------------------
Path: ros2_ws/src/astro_base/setup.py
```python
from setuptools import setup
from glob import glob

package_name = 'astro_base'

setup(
    name=package_name,
    version='0.1.0',
    packages=[package_name],
    data_files=[
        ('share/ament_index/resource_index/packages', ['resource/' + package_name]),
        ('share/' + package_name, ['package.xml']),
    ],
    install_requires=['setuptools', 'pyserial'],
    zip_safe=True,
    author='ASTRO Dev',
    author_email='dev@example.com',
    description='ASTRO V1 base nodes and interfaces',
    license='Apache-2.0',
    entry_points={
        'console_scripts': [
            'serial_bridge = astro_base.serial_bridge:main',
        ],
    },
)
```

-------------------------------------------------------------------
Path: ros2_ws/src/astro_base/resource/astro_base
```txt
# ament resource index placeholder
```

-------------------------------------------------------------------
Path: ros2_ws/src/astro_base/astro_base/__init__.py
```python
# Package init
```

-------------------------------------------------------------------
Path: ros2_ws/src/astro_base/src/serial_bridge.py
```python
#!/usr/bin/env python3
import struct
import time
import threading
import serial

import rclpy
from rclpy.node import Node
from rclpy.qos import QoSProfile, ReliabilityPolicy

from sensor_msgs.msg import Imu, JointState
from diagnostic_msgs.msg import DiagnosticArray, DiagnosticStatus, KeyValue

from astro_base.msg import WheelCmd, HeadCmd

SOF1 = 0xAA
SOF2 = 0x55

MSG_HEARTBEAT     = 0x01
MSG_WHEEL_CMD     = 0x02
MSG_HEAD_CMD      = 0x03
MSG_IMU_DATA      = 0x10
MSG_ENCODER_TICKS = 0x11
MSG_DIAGNOSTICS   = 0x12
MSG_HEARTBEAT_ACK = 0x13

def crc8(data: bytes) -> int:
    poly = 0x07
    crc = 0x00
    for b in data:
        crc ^= b
        for _ in range(8):
            if crc & 0x80:
                crc = ((crc << 1) & 0xFF) ^ poly
            else:
                crc = (crc << 1) & 0xFF
    return crc

class SerialBridge(Node):
    def __init__(self):
        super().__init__('serial_bridge')
        self.declare_parameter('port', '/dev/ttyACM0')
        self.declare_parameter('baud', 500000)
        self.declare_parameter('frame_id_imu', 'imu_link')
        self.declare_parameter('ticks_per_rev_left', 2048.0)
        self.declare_parameter('ticks_per_rev_right', 2048.0)
        self.declare_parameter('wheel_radius_left', 0.06)
        self.declare_parameter('wheel_radius_right', 0.06)

        self.port = self.get_parameter('port').get_parameter_value().string_value
        self.baud = self.get_parameter('baud').get_parameter_value().integer_value

        self.frame_id_imu = self.get_parameter('frame_id_imu').get_parameter_value().string_value
        self.tpr_l = float(self.get_parameter('ticks_per_rev_left').value)
        self.tpr_r = float(self.get_parameter('ticks_per_rev_right').value)
        self.r_l = float(self.get_parameter('wheel_radius_left').value)
        self.r_r = float(self.get_parameter('wheel_radius_right').value)

        self.ser = serial.Serial(self.port, self.baud, timeout=0.01)
        self.get_logger().info(f'Opened serial {self.port} @ {self.baud}')

        qos_best_effort = QoSProfile(depth=10, reliability=ReliabilityPolicy.BEST_EFFORT)

        self.pub_imu = self.create_publisher(Imu, '/imu/data_raw', qos_best_effort)
        self.pub_js = self.create_publisher(JointState, '/joint_states', 10)
        self.pub_diag = self.create_publisher(DiagnosticArray, '/arduino/diagnostics', 10)

        self.sub_wheel = self.create_subscription(WheelCmd, '/wheel_cmds', self.on_wheel_cmd, 10)
        self.sub_head = self.create_subscription(HeadCmd, '/head_cmd', self.on_head_cmd, 10)

        self.hb_timer = self.create_timer(0.1, self.send_heartbeat)  # 10 Hz
        self.last_hb_ack = self.get_clock().now()

        self.parser_lock = threading.Lock()
        self.rx_thread = threading.Thread(target=self.read_loop, daemon=True)
        self.rx_thread.start()

        # Durum
        self.left_ticks = 0
        self.right_ticks = 0
        self.left_pos = 0.0
        self.right_pos = 0.0

    def build_packet(self, msg_id: int, payload: bytes) -> bytes:
        length = 1 + len(payload)
        body = bytes([length, msg_id]) + payload
        c = crc8(body)
        return bytes([SOF1, SOF2]) + body + bytes([c])

    def send_heartbeat(self):
        pkt = self.build_packet(MSG_HEARTBEAT, b'')
        try:
            self.ser.write(pkt)
        except Exception as e:
            self.get_logger().warn(f'Heartbeat write failed: {e}')

        # Watch if no ack for > 1s -> diag warn
        if (self.get_clock().now() - self.last_hb_ack).nanoseconds > 1_000_000_000:
            self.get_logger().warn('No heartbeat ACK from Arduino >1s')

    def on_wheel_cmd(self, msg: WheelCmd):
        payload = struct.pack('<ff', msg.left_rpm, msg.right_rpm)
        pkt = self.build_packet(MSG_WHEEL_CMD, payload)
        try:
            self.ser.write(pkt)
        except Exception as e:
            self.get_logger().error(f'WheelCmd write failed: {e}')

    def on_head_cmd(self, msg: HeadCmd):
        payload = struct.pack('<f', msg.angle_deg)
        pkt = self.build_packet(MSG_HEAD_CMD, payload)
        try:
            self.ser.write(pkt)
        except Exception as e:
            self.get_logger().error(f'HeadCmd write failed: {e}')

    def publish_imu(self, ax, ay, az, gx, gy, gz, stamp_ns: int):
        m = Imu()
        # Zaman damgası: seri paketten micros geldi -> şimdiye göre dönüştür (yaklaşık)
        now = self.get_clock().now()
        m.header.stamp = now.to_msg()
        m.header.frame_id = self.frame_id_imu
        m.linear_acceleration.x = ax
        m.linear_acceleration.y = ay
        m.linear_acceleration.z = az
        m.angular_velocity.x = gx
        m.angular_velocity.y = gy
        m.angular_velocity.z = gz
        # Kovaryansları örnek olarak bilinmiyor -> -1
        m.linear_acceleration_covariance[0] = -1.0
        m.angular_velocity_covariance[0] = -1.0
        self.pub_imu.publish(m)

    def publish_joint_states(self, dl: int, dr: int, dt_us: int):
        # Ticks -> rad
        dtheta_l = (dl / self.tpr_l) * 2.0 * 3.141592653589793
        dtheta_r = (dr / self.tpr_r) * 2.0 * 3.141592653589793
        self.left_pos += dtheta_l
        self.right_pos += dtheta_r

        js = JointState()
        js.header.stamp = self.get_clock().now().to_msg()
        js.name = ['left_wheel_joint', 'right_wheel_joint', 'head_yaw_joint']
        js.position = [self.left_pos, self.right_pos, float('nan')]
        self.pub_js.publish(js)

    def publish_diag(self, vbat_mV: int, temp_cX100: int, flags: int):
        da = DiagnosticArray()
        da.header.stamp = self.get_clock().now().to_msg()
        st = DiagnosticStatus()
        st.name = 'arduino'
        st.hardware_id = 'astro_arduino_mega'
        st.level = DiagnosticStatus.OK
        st.message = 'OK'

        if flags & 0x01:
            st.level = DiagnosticStatus.WARN
            st.message = 'MOTORS_DISABLED_WATCHDOG'

        st.values = [
            KeyValue(key='vbat_mV', value=str(vbat_mV)),
            KeyValue(key='mcu_temp_c', value=str(temp_cX100/100.0)),
            KeyValue(key='flags', value=hex(flags)),
        ]
        da.status = [st]
        self.pub_diag.publish(da)

    def read_loop(self):
        state = 0
        expected_len = 0
        buf = bytearray()
        while rclpy.ok():
            try:
                b = self.ser.read(1)
                if not b:
                    continue
                b = b[0]
                if state == 0:
                    if b == SOF1: state = 1
                elif state == 1:
                    if b == SOF2: state = 2
                    else: state = 0
                elif state == 2:
                    expected_len = b
                    buf = bytearray()
                    state = 3
                elif state == 3:
                    buf.append(b)
                    if len(buf) >= expected_len:
                        state = 4
                elif state == 4:
                    # CRC check
                    body = bytes([expected_len]) + bytes(buf)
                    c = crc8(body)
                    if c == b:
                        msg_id = buf[0]
                        payload = bytes(buf[1:])
                        self.handle_msg(msg_id, payload)
                    else:
                        self.get_logger().warn('CRC mismatch')
                    state = 0
            except Exception as e:
                self.get_logger().error(f'Serial read error: {e}')
                time.sleep(0.1)

    def handle_msg(self, msg_id: int, payload: bytes):
        if msg_id == MSG_IMU_DATA:
            if len(payload) != 6*4 + 4: return
            ax, ay, az, gx, gy, gz, micros_ts = struct.unpack('<ffffffI', payload)
            self.publish_imu(ax, ay, az, gx, gy, gz, micros_ts)
        elif msg_id == MSG_ENCODER_TICKS:
            if len(payload) != 12: return
            l, r, dt_us = struct.unpack('<iiI', payload)
            self.publish_joint_states(l, r, dt_us)
        elif msg_id == MSG_DIAGNOSTICS:
            if len(payload) != 8: return
            vbat_mV, temp_cX100, flags = struct.unpack('<HHi', payload + b'\x00\x00')[:3]
            # Not: payload 2+2+4 = 8B, struct ile uyum için açık unpack:
            vbat_mV = int.from_bytes(payload[0:2], 'little', signed=False)
            temp_cX100 = int.from_bytes(payload[2:4], 'little', signed=True)
            flags = int.from_bytes(payload[4:8], 'little', signed=False)
            self.publish_diag(vbat_mV, temp_cX100, flags)
        elif msg_id == MSG_HEARTBEAT_ACK:
            self.last_hb_ack = self.get_clock().now()

def main():
    rclpy.init()
    node = SerialBridge()
    try:
        rclpy.spin(node)
    except KeyboardInterrupt:
        pass
    node.destroy_node()
    rclpy.shutdown()

if __name__ == '__main__':
    main()
```

-------------------------------------------------------------------
Path: ros2_ws/src/astro_description/package.xml
```xml
<?xml version="1.0"?>
<package format="3">
  <name>astro_description</name>
  <version>0.1.0</version>
  <description>ASTRO V1 robot description (URDF/Xacro)</description>
  <maintainer email="dev@example.com">ASTRO Dev</maintainer>
  <license>Apache-2.0</license>
  <buildtool_depend>ament_cmake</buildtool_depend>
  <exec_depend>xacro</exec_depend>
  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
```

-------------------------------------------------------------------
Path: ros2_ws/src/astro_description/CMakeLists.txt
```cmake
cmake_minimum_required(VERSION 3.10)
project(astro_description)
find_package(ament_cmake REQUIRED)

install(DIRECTORY urdf
  DESTINATION share/${PROJECT_NAME}
)

ament_package()
```

-------------------------------------------------------------------
Path: ros2_ws/src/astro_description/urdf/astro.urdf.xacro
```xml
<?xml version="1.0"?>
<robot name="astro" xmlns:xacro="http://ros.org/wiki/xacro">
  <xacro:macro name="inertial_box" params="mass x y z">
    <inertial>
      <mass value="${mass}"/>
      <origin xyz="0 0 0"/>
      <inertia ixx="0.1" ixy="0.0" ixz="0.0" iyy="0.1" iyz="0.0" izz="0.1"/>
    </inertial>
  </xacro:macro>

  <link name="base_link">
    <visual>
      <geometry><box size="0.3 0.2 0.1"/></geometry>
    </visual>
    <xacro:inertial_box mass="5.0" x="0.3" y="0.2" z="0.1"/>
  </link>

  <link name="left_wheel_link">
    <visual><geometry><cylinder radius="0.06" length="0.02"/></geometry></visual>
  </link>
  <joint name="left_wheel_joint" type="continuous">
    <parent link="base_link"/>
    <child link="left_wheel_link"/>
    <origin xyz="0 0.1 0"/>
    <axis xyz="0 1 0"/>
  </joint>

  <link name="right_wheel_link">
    <visual><geometry><cylinder radius="0.06" length="0.02"/></geometry></visual>
  </link>
  <joint name="right_wheel_joint" type="continuous">
    <parent link="base_link"/>
    <child link="right_wheel_link"/>
    <origin xyz="0 -0.1 0"/>
    <axis xyz="0 1 0"/>
  </joint>

  <link name="head_link">
    <visual><geometry><cylinder radius="0.03" length="0.05"/></geometry></visual>
  </link>
  <joint name="head_yaw_joint" type="continuous">
    <parent link="base_link"/>
    <child link="head_link"/>
    <origin xyz="0.1 0 0.1"/>
    <axis xyz="0 0 1"/>
  </joint>
</robot>
```

-------------------------------------------------------------------
Path: ros2_ws/src/astro_bringup/package.xml
```xml
<?xml version="1.0"?>
<package format="3">
  <name>astro_bringup</name>
  <version>0.1.0</version>
  <description>ASTRO V1 bringup launch files</description>
  <maintainer email="dev@example.com">ASTRO Dev</maintainer>
  <license>Apache-2.0</license>
  <buildtool_depend>ament_cmake</buildtool_depend>
  <depend>astro_base</depend>
  <depend>astro_description</depend>
  <depend>robot_state_publisher</depend>
  <depend>joint_state_publisher</depend>
  <depend>xacro</depend>
  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
```

-------------------------------------------------------------------
Path: ros2_ws/src/astro_bringup/CMakeLists.txt
```cmake
cmake_minimum_required(VERSION 3.10)
project(astro_bringup)
find_package(ament_cmake REQUIRED)

install(DIRECTORY launch
  DESTINATION share/${PROJECT_NAME}
)

ament_package()
```

-------------------------------------------------------------------
Path: ros2_ws/src/astro_bringup/launch/bringup.launch.py
```python
from launch import LaunchDescription
from launch.actions import DeclareLaunchArgument
from launch.substitutions import LaunchConfiguration, Command
from launch_ros.actions import Node
import os

def generate_launch_description():
    port_arg = DeclareLaunchArgument('port', default_value='/dev/ttyACM0')
    baud_arg = DeclareLaunchArgument('baud', default_value='500000')

    urdf = Command(['xacro ', 
                    os.path.join(
                        os.getenv('AMENT_PREFIX_PATH').split(':')[0],
                        'share', 'astro_description', 'urdf', 'astro.urdf.xacro')])

    rsp = Node(
        package='robot_state_publisher',
        executable='robot_state_publisher',
        name='robot_state_publisher',
        output='screen',
        parameters=[{'robot_description': urdf}]
    )

    js = Node(
        package='joint_state_publisher',
        executable='joint_state_publisher',
        name='joint_state_publisher',
        output='screen'
    )

    serial = Node(
        package='astro_base',
        executable='serial_bridge',
        name='serial_bridge',
        output='screen',
        parameters=[{
            'port': LaunchConfiguration('port'),
            'baud': LaunchConfiguration('baud'),
            'frame_id_imu': 'imu_link',
            'ticks_per_rev_left': 2048.0,
            'ticks_per_rev_right': 2048.0,
            'wheel_radius_left': 0.06,
            'wheel_radius_right': 0.06
        }]
    )

    return LaunchDescription([port_arg, baud_arg, rsp, js, serial])
```

-------------------------------------------------------------------
Önemli Uygulama Notları:
- ROS 2 tarafı derleme (Jetson Orin Nano, Humble Rolling fork bağlamı):
  - Colcon ile: colcon build --symlink-install, sonra source install/setup.bash
  - astro_base içinde hem rosidl arayüzleri hem Python node tek pakette ament_cmake + ament_cmake_python ile sağlandı.
  - Mevcut rosidl monorepo fork’unuzda özel buffer paketleri varsa, ek ara yüzleri bu projeye taşımak gerekmez; burada standart ROS 2 msg’ler ve CRC-8’li seri protokol kullanıldı. İleride rosidl_buffer tabanlı bir backend eklemek isterseniz, serial_bridge.py içine opsiyonel bir “backend” katmanı ekleyebilirsiniz.

- Arduino derleme:
  - PlatformIO: pio run -t upload, pio device monitor -b 500000
  - Donanım WD: 1s. Yazılım WD: 500 ms heartbeat (host’tan Heartbeat/komut gelmezse motorlar kesilir).
  - Enkoder pinleri Mega’nın harici kesmelerine uygun seçildi (A kanalı 2 ve 3). B kanalı normal GPIO’dan okunarak yön tayini yapılıyor.

- Motor PID (50 Hz):
  - Basit PI(D) kontrol; RPM hedefleri /wheel_cmds üzerinden gelir. Gerekirse KP/KI parametrelerini seri protokole ekleyip ROS paramları ile güncellenebilir.

- TMC2209:
  - UART üzerinden akım ve mikrostep ayarlandı (TMCStepper).
  - Hareket AccelStepper ile step/dir üzerinden (target açı → steps). Kalibrasyonu steps/degree üzerinden ayarladık.

- IMU:
  - Örnek MPU-6050. OAK-D-Lite IMU erişimi Jetson’dan USB ile yapılacaksa, Arduino yerine Jetson tarafında OAK-SDK ile /imu verisi üretilebilir. Bu kodda istenen gereksinime uygun olarak I2C’den 6 eksen okuma sağlandı.

- Güvenilirlik:
  - CRC-8-ATM kontrolü, senkronizasyon için 0xAA 0x55 başlık, uzunluk+ID tabanlı sabit çerçeveleme.
  - Hata durumunda parser reset.
  - DiagnosticArray ile durum raporu (/arduino/diagnostics).

- ROS Topic eşlemesi:
  - /wheel_cmds: astro_base/WheelCmd (left_rpm, right_rpm).
  - /head_cmd: astro_base/HeadCmd (angle_deg).
  - /joint_states: JointState (wheels pozisyonları tick→rad entegrasyon).
  - /imu/data_raw: sensor_msgs/Imu (accel [m/s²], gyro [rad/s]).
  - /arduino/diagnostics: DiagnosticArray.

Bu iskelet, üretim kalitesine yakın; pinler, akım/mikrostep, PID kazançları, enkoder çözünürlüğü, teker yarıçapı ve seri port isimleri sahaya göre ayarlanmalıdır. Ayrıca OAK-D-Lite entegrasyonu için Jetson tarafında ayrı bir node ile IMU/vision beslemesi yapılabilir.


## AŞAMA 2 — Claude (Kod İnceleyici): İnceleme Raporu

*Model: `LLMName.CLAUDE_V4_5_SONNET`*

# KOD İNCELEME RAPORU: ASTRO V1 ROS2-Arduino Entegrasyonu

## GENEL DEĞERLENDİRME

Bu proje, ROS2 Humble ve Arduino Mega arasında seri haberleşme tabanlı bir robot kontrol sistemi iskeletidir. Genel mimari sağlam, ancak **ÜRETİM ORTAMINA GEÇİŞ İÇİN ÖNEMLİ DÜZELTMELER GEREKMEKTEDİR**.

---

## KRİTİK SORUNLAR

### 1. **CRC-8 Hesaplama Hatası (Arduino - protocol.h)** - **KRİTİK**
**Dosya:** `arduino/astro_firmware/include/protocol.h` (satır 33-42)

```cpp
case WAIT_CRC: {
  uint8_t crc = crc8(&len, 1); // YANLIŞ!
  crc = crc8(data, len);       // YANLIŞ! Önceki CRC üzerine yazmış
  // ...
  uint8_t tmp[1 + 255];
  tmp[0] = len;
  memcpy(&tmp[1], data, len);
  uint8_t calc = crc8(tmp, 1 + len); // DOĞRU hesaplama
```

**Sorun:** İlk iki satır gereksiz ve yanlış; doğru hesaplama zaten yapılmış ama kod karmaşık.

**Çözüm:**
```cpp
case WAIT_CRC: {
  uint8_t tmp[1 + 255];
  tmp[0] = len;
  memcpy(&tmp[1], data, len);
  uint8_t calc = crc8(tmp, 1 + len);
  if (calc == b) {
    // ...
  }
}
```

---

### 2. **Watchdog Timeout Riski (Arduino - main.cpp)** - **KRİTİK**
**Dosya:** `arduino/astro_firmware/src/main.cpp` (satır 178-180)

```cpp
wdt_enable(WDTO_1S); // 1 saniye HW watchdog
```

**Sorun:** `loopControl()` 50 Hz (20 ms) çalışıyor, ancak:
- IMU I2C okuma takılırsa (Wire.requestFrom timeout yok)
- Seri yazma bloklanırsa
- TMC2209 UART başlatma 50 ms delay içeriyor

1 saniye yeterli **görünse de**, I2C/UART hataları birikirse `wdt_reset()` çağrısı gecikebilir.

**Çözüm:**
```cpp
wdt_enable(WDTO_2S); // Güvenlik marjı
// veya kritik bölümlerde ek wdt_reset() ekle
```

---

### 3. **I2C Timeout Eksikliği (Arduino - main.cpp)** - **KRİTİK**
**Dosya:** `arduino/astro_firmware/src/main.cpp` (satır 99-102)

```cpp
Wire.beginTransmission(IMU_I2C_ADDR);
Wire.write(0x3B);
if (Wire.endTransmission(false) != 0) return false;
uint8_t n = Wire.requestFrom(IMU_I2C_ADDR, 14u, true);
```

**Sorun:** `Wire.requestFrom()` I2C bus takılırsa **sonsuz bekleyebilir** (AVR Wire kütüphanesi timeout yok).

**Çözüm:**
```cpp
Wire.setWireTimeout(5000, true); // 5 ms timeout (setup'ta bir kez)
// veya
unsigned long t0 = millis();
while (Wire.available() < 14 && millis() - t0 < 10) {}
if (Wire.available() < 14) return false;
```

---

### 4. **Enkoder Kesme Yarış Durumu (Arduino - main.cpp)** - **KRİTİK**
**Dosya:** `arduino/astro_firmware/src/main.cpp` (satır 60-68, 155-159)

```cpp
volatile int32_t g_left_ticks = 0;
// ...
void IRAM_ATTR leftEncA() {
  bool b = digitalRead(L_ENC_B);
  g_left_ticks += b ? -1 : +1; // Atomik değil!
}
// ...
int32_t l_ticks = g_left_ticks; // Ana döngüde okuma
```

**Sorun:** 
- `int32_t` okuma/yazma AVR'de atomik **DEĞİL** (8-bit MCU).
- Kesme sırasında ana döngü `g_left_ticks` okursa **yırtık okuma** (torn read) olabilir.

**Çözüm:**
```cpp
int32_t l_ticks;
noInterrupts();
l_ticks = g_left_ticks;
interrupts();
```

---

### 5. **PID Anti-Windup Yetersiz (Arduino - main.cpp)** - **ORTA**
**Dosya:** `arduino/astro_firmware/src/main.cpp` (satır 167-175)

```cpp
e_i += e * (dt_ms / 1000.0f);
e_i = constrain(e_i, -200.0f, 200.0f); // Sabit limit
```

**Sorun:** 
- Anti-windup limiti sabit (-200..200), PWM çıkışı ile ilişkisiz.
- Motor dururken (target=0, meas≠0) integral hızla birikir.

**Çözüm:**
```cpp
// PWM saturasyon durumunda integral durdur (conditional integration)
if (abs(pwm) < PWM_MAX) {
  e_i += e * (dt_ms / 1000.0f);
}
e_i = constrain(e_i, -50.0f, 50.0f); // Daha dar limit
```

---

### 6. **Seri Buffer Taşması Riski (Python - serial_bridge.py)** - **ORTA**
**Dosya:** `ros2_ws/src/astro_base/src/serial_bridge.py` (satır 143-145)

```python
def read_loop(self):
    # ...
    b = self.ser.read(1)
    if not b:
        continue
```

**Sorun:** 
- `timeout=0.01` ile 10 ms bloklanıyor, ancak yüksek hızda (500 kbps) buffer dolabilir.
- `pyserial` buffer varsayılan 4096 byte; Arduino 50 Hz × 50 byte/paket = 2.5 KB/s → 1.6 saniyede dolar.

**Çözüm:**
```python
b = self.ser.read(self.ser.in_waiting or 1) # Mevcut tüm veriyi oku
for byte in b:
    # Parser'a besle
```

---

### 7. **Zaman Damgası Senkronizasyonu Eksik (Python - serial_bridge.py)** - **ORTA**
**Dosya:** `ros2_ws/src/astro_base/src/serial_bridge.py` (satır 105-108)

```python
def publish_imu(self, ax, ay, az, gx, gy, gz, stamp_ns: int):
    # ...
    now = self.get_clock().now()
    m.header.stamp = now.to_msg() # Arduino'dan gelen micros kullanılmıyor!
```

**Sorun:** Arduino'dan gelen `micros()` zaman damgası göz ardı ediliyor; ROS zaman damgası ile IMU ölçümü arasında 10-50 ms gecikme olabilir.

**Çözüm:**
```python
# Başlangıçta offset hesapla
self.time_offset = self.get_clock().now().nanoseconds - (stamp_ns * 1000)
# Publish'te:
stamp_ros_ns = (stamp_ns * 1000) + self.time_offset
m.header.stamp = rclpy.time.Time(nanoseconds=stamp_ros_ns).to_msg()
```

---

### 8. **TMC2209 UART Başlatma Gecikmesi (Arduino - main.cpp)** - **ORTA**
**Dosya:** `arduino/astro_firmware/src/main.cpp` (satır 131-133)

```cpp
TMC2209_SERIAL.begin(115200);
delay(50); // Bloklanıyor!
tmc2209.begin();
```

**Sorun:** `setup()` içinde 50 ms delay, watchdog başlamadan önce olsa da kötü pratik.

**Çözüm:**
```cpp
// Non-blocking başlatma veya watchdog'u setup sonunda enable et
```

---

## ORTA ÖNCELİKLİ SORUNLAR

### 9. **Enkoder Yön Tespiti Basitleştirilmiş** - **ORTA**
**Dosya:** `arduino/astro_firmware/src/main.cpp` (satır 60-68)

```cpp
void IRAM_ATTR leftEncA() {
  bool b = digitalRead(L_ENC_B);
  g_left_ticks += b ? -1 : +1;
}
```

**Sorun:** Tek kenar (RISING) ile yön tespiti; gürültüye karşı hassas. Quadrature dekodlama için her iki kenar (A ve B) izlenmeli.

**Öneri:**
```cpp
// Tam quadrature: A ve B'nin her iki kenarını izle (4x çözünürlük)
// veya harici quadrature decoder IC kullan
```

---

### 10. **Motor PWM Frekansı Belirtilmemiş** - **ORTA**
**Dosya:** `arduino/astro_firmware/src/main.cpp` (satır 195-198)

```cpp
// PWM frekansları varsayılan; istenirse timer ayarları optimize edilebilir
analogWrite(L_MOTOR_PWM_FWD, 0);
```

**Sorun:** Arduino Mega varsayılan PWM frekansı ~490 Hz (pinlere göre 980 Hz); BTS7960 için düşük olabilir (motor gürültüsü).

**Öneri:**
```cpp
// Timer1 (pin 11,12): 31.25 kHz
TCCR1B = (TCCR1B & 0xF8) | 0x01; // prescaler=1
```

---

### 11. **Diagnostik Struct Packing Hatası (Python - serial_bridge.py)** - **ORTA**
**Dosya:** `ros2_ws/src/astro_base/src/serial_bridge.py` (satır 173-177)

```python
vbat_mV, temp_cX100, flags = struct.unpack('<HHi', payload + b'\x00\x00')[:3]
# Sonra manuel unpack:
vbat_mV = int.from_bytes(payload[0:2], 'little', signed=False)
```

**Sorun:** Kod çelişkili; `struct.unpack` sonucu kullanılmıyor, manuel unpack yapılıyor. Arduino tarafı 2+2+4=8 byte gönderiyor, Python 12 byte bekliyor (`HHi` = 2+2+4, ancak padding olabilir).

**Çözüm:**
```python
if len(payload) != 8: return
vbat_mV, temp_cX100, flags = struct.unpack('<HhI', payload)
```

---

### 12. **ROS2 QoS Profili Tutarsızlığı** - **ORTA**
**Dosya:** `ros2_ws/src/astro_base/src/serial_bridge.py` (satır 49-51)

```python
qos_best_effort = QoSProfile(depth=10, reliability=ReliabilityPolicy.BEST_EFFORT)
self.pub_imu = self.create_publisher(Imu, '/imu/data_raw', qos_best_effort)
self.pub_js = self.create_publisher(JointState, '/joint_states', 10) # Varsayılan RELIABLE
```

**Sorun:** IMU için BEST_EFFORT, JointState için RELIABLE; tutarlılık yok. Genelde sensör verileri BEST_EFFORT, kontrol komutları RELIABLE olmalı.

**Öneri:**
```python
# Tüm sensör yayınları için BEST_EFFORT
self.pub_js = self.create_publisher(JointState, '/joint_states', qos_best_effort)
```

---

### 13. **Heartbeat ACK Timeout Kontrolü Zayıf** - **ORTA**
**Dosya:** `ros2_ws/src/astro_base/src/serial_bridge.py` (satır 88-91)

```python
if (self.get_clock().now() - self.last_hb_ack).nanoseconds > 1_000_000_000:
    self.get_logger().warn('No heartbeat ACK from Arduino >1s')
```

**Sorun:** Sadece log basıyor, aksiyon almıyor (örn. motor komutlarını kesme).

**Öneri:**
```python
self.arduino_alive = False
# WheelCmd gönderme öncesi kontrol et
```

---

## DÜŞÜK ÖNCELİKLİ SORUNLAR

### 14. **Magic Number Kullanımı** - **DÜŞÜK**
**Dosya:** Birçok yerde (örn. `main.cpp` satır 42-45)

```cpp
static constexpr int32_t TICKS_PER_REV_L = 2048;
static constexpr float WHEEL_R_L = 0.06f;
static constexpr float KP = 0.6f, KI = 0.2f, KD = 0.0f;
```

**Sorun:** Sabit kodlanmış değerler; kalibrasyon için yeniden derleme gerekiyor.

**Öneri:** ROS parametreleri üzerinden gönder veya EEPROM'da sakla.

---

### 15. **Hata Loglaması Eksik (Arduino)** - **DÜŞÜK**
**Dosya:** `arduino/astro_firmware/src/main.cpp`

**Sorun:** I2C/UART hataları sessizce göz ardı ediliyor; debug zor.

**Öneri:**
```cpp
if (!imuRead()) {
  // Diagnostik flags'e hata biti ekle
  g_diag_flags |= 0x02; // IMU_READ_FAIL
}
```

---

### 16. **URDF Eksik Parametreler** - **DÜŞÜK**
**Dosya:** `ros2_ws/src/astro_description/urdf/astro.urdf.xacro` (satır 5-9)

```xml
<inertia ixx="0.1" ixy="0.0" ixz="0.0" iyy="0.1" iyz="0.0" izz="0.1"/>
```

**Sorun:** Atalet tensörü placeholder değerler; simülasyon/dinamik hesaplamalar yanlış olur.

**Öneri:** Gerçek CAD modelinden hesapla veya `meshlab`/`solidworks` kullan.

---

### 17. **Launch Dosyası AMENT_PREFIX_PATH Varsayımı** - **DÜŞÜK**
**Dosya:** `ros2_ws/src/astro_bringup/launch/bringup.launch.py` (satır 11-13)

```python
urdf = Command(['xacro ', 
                os.path.join(
                    os.getenv('AMENT_PREFIX_PATH').split(':')[0],
```

**Sorun:** `AMENT_PREFIX_PATH` birden fazla yol içerebilir; ilkinin `astro_description` içerdiği garanti değil.

**Çözüm:**
```python
from ament_index_python.packages import get_package_share_directory
urdf_path = os.path.join(get_package_share_directory('astro_description'), 
                         'urdf', 'astro.urdf.xacro')
```

---

### 18. **Python Setup.py Eksik Bağımlılık** - **DÜŞÜK**
**Dosya:** `ros2_ws/src/astro_base/setup.py` (satır 11)

```python
install_requires=['setuptools', 'pyserial'],
```

**Sorun:** `pyserial` pip bağımlılığı; ROS2'de `rosdep` ile yönetilmeli.

**Çözüm:** `package.xml`'e ekle:
```xml
<depend>python3-serial</depend>
```

---

### 19. **CRC-8 Polinom Standardı Belirtilmemiş** - **DÜŞÜK**
**Dosya:** `arduino/astro_firmware/include/protocol.h` (satır 15)

```cpp
// CRC8 polinom: 0x07, init 0x00 (CRC-8-ATM)
```

**Sorun:** Yorum doğru ama kod içinde init değeri açık değil (fonksiyon içinde `0x00` başlatılıyor).

**Öneri:** Daha standart CRC-8/MAXIM (0x31) veya CRC-8/CCITT kullan; test vektörleri ekle.

---

### 20. **Enkoder Pozisyon Entegrasyonu Drift** - **DÜŞÜK**
**Dosya:** `ros2_ws/src/astro_base/src/serial_bridge.py` (satır 118-122)

```python
dtheta_l = (dl / self.tpr_l) * 2.0 * 3.141592653589793
self.left_pos += dtheta_l
```

**Sorun:** Uzun süreli çalışmada float toplama hatası birikir.

**Öneri:** Periyodik olarak sıfırla veya `math.fsum()` kullan.

---

## GÜVENLİK DEĞERLENDİRMESİ

### ✅ Güçlü Yönler:
1. **CRC-8 Paket Doğrulama:** Seri haberleşme bozuk paketlere karşı korumalı.
2. **Watchdog Mekanizması:** Hem HW (1s) hem SW (500 ms heartbeat) mevcut.
3. **Motor Güvenlik Kilidi:** Heartbeat timeout'unda motorlar otomatik kesiliyor.

### ⚠️ Zayıf Yönler:
1. **Atomik Olmayan Enkoder Okuma:** Veri bütünlüğü riski.
2. **I2C Timeout Yok:** Bus takılması sistemin donmasına yol açabilir.
3. **Zaman Senkronizasyonu Eksik:** IMU-ROS zaman uyumsuzluğu.

---

## PERFORMANS DEĞERLENDİRMESİ

### Güçlü Yönler:
- **50 Hz Kontrol Döngüsü:** Motor kontrolü için yeterli.
- **500 kbps Seri Hız:** Bandwidth yeterli (50 Hz × 50 byte = 2.5 KB/s << 62.5 KB/s).

### İyileştirme Alanları:
- **PWM Frekansı:** 490 Hz → 20 kHz+ (motor gürültüsü azalır).
- **Enkoder Çözünürlüğü:** Tek kenar → quadrature (4x hassasiyet).
- **PID Tuning:** Sabit kazançlar; adaptif veya parametre tabanlı olmalı.

---

## OKUNABİLİRLİK & BAKIM

### ✅ İyi Pratikler:
- Modüler yapı (pins.h, protocol.h ayrımı).
- ROS2 standart mesaj tipleri kullanımı.
- Yorum satırları mevcut.

### ⚠️ İyileştirme Gereken:
- **Sihirli Sayılar:** Konfigürasyon dosyasına taşınmalı.
- **Hata Yönetimi:** Try-catch blokları eksik (Python tarafı).
- **Test Kodu Yok:** Unit test veya integration test mevcut değil.

---

## ROS2/AMENT UYUMLULUĞU

### ✅ Uygun:
- `ament_cmake` + `ament_cmake_python` karma yapı doğru.
- `rosidl_generate_interfaces` kullanımı standart.
- Launch dosyası Python API ile yazılmış.

### ⚠️ Sorunlu:
- **AMENT_PREFIX_PATH Kullanımı:** `ament_index_python` tercih edilmeli.
- **setup.py Bağımlılıkları:** `rosdep` ile yönetilmeli.

---

## ROSIDL MONOREPO BAĞLAMI

**Önemli Not:** Proje açıklamasında "rosidl monorepo fork'u" ve "özel buffer paketleri" (rosidl_buffer, rosidl_buffer_backend) geliştirildiği belirtilmiş. **Ancak bu projede hiçbir özel rosidl backend kullanılmamış**; standart ROS2 mesajları (sensor_msgs, diagnostic_msgs) ve custom msg'ler (WheelCmd, HeadCmd) kullanılmış.

### Entegrasyon Önerileri:
1. **rosidl_buffer Backend:** Eğer seri protokol için özel bir IDL backend geliştirildiyse, `protocol.h` CRC-8 yapısı bu backend'e taşınabilir.
2. **Kod Üretimi:** `.msg` dosyalarından Arduino C++ struct'ları otomatik üretilebilir (rosidl_generator_cpp benzeri).
3. **Sıfır Kopya Serileştirme:** `rosidl_buffer` ile doğrudan DMA buffer'larına yazma mümkün olabilir.

**Mevcut Durum:** Proje, rosidl monorepo'dan **bağımsız** çalışıyor; entegrasyon için ek çalışma gerekli.

---

## ÖNERİLEN DÜZELTME SIRASI

### Faz 1 (Acil - Üretim Öncesi):
1. ✅ Enkoder okuma atomik yap (`noInterrupts()`).
2. ✅ I2C timeout ekle (`Wire.setWireTimeout()`).
3. ✅ CRC hesaplama kodunu temizle.
4. ✅ Diagnostik struct packing düzelt.

### Faz 2 (Kısa Vadeli):
5. ✅ Zaman senkronizasyonu ekle (IMU timestamp).
6. ✅ PID anti-windup iyileştir.
7. ✅ Seri buffer taşması önle (bulk read).
8. ✅ Watchdog timeout'u 2s'ye çıkar.

### Faz 3 (Orta Vadeli):
9. ✅ PWM frekansını artır (20 kHz).
10. ✅ Quadrature enkoder dekodlama ekle.
11. ✅ ROS parametreleri ile konfigürasyon.
12. ✅ Unit testler yaz (gtest + pytest).

### Faz 4 (Uzun Vadeli):
13. ✅ rosidl_buffer backend entegrasyonu.
14. ✅ OAK-D-Lite IMU entegrasyonu (Jetson tarafı).
15. ✅ Gerçek URDF inertia değerleri.

---

## SONUÇ

### Genel Puan: **6.5/10**

**Güçlü Yönler:**
- Mimari sağlam ve modüler.
- Watchdog ve CRC koruması mevcut.
- ROS2 standartlarına genel uyum.

**Kritik Eksikler:**
- Atomik olmayan enkoder okuma (veri bütünlüğü riski).
- I2C timeout eksikliği (sistem donma riski).
- Zaman senkronizasyonu yok (sensör füzyonu sorunlu).

**Karar:** **DÜZELTME GEREKLİ**

### Üretim Onayı İçin Gerekli Minimum Düzeltmeler:
1. Enkoder okuma atomik yapılmalı.
2. I2C timeout eklenmeli.
3. CRC hesaplama kodu temizlenmeli.
4. Diagnostik struct packing düzeltilmeli.
5. Watchdog timeout marjı artırılmalı.

**Bu düzeltmeler yapıldıktan sonra:** Saha testleri ile PID tuning ve enkoder kalibrasyonu yapılmalı; ardından üretim onayı verilebilir.

---

## EK NOTLAR

### Test Önerileri:
```bash
# Arduino tarafı:
# 1. Enkoder gürültü testi (motor kapalı, tekerlek elle çevir)
# 2. I2C bus stress testi (IMU kablosunu çek/tak)
# 3. Watchdog testi (seri kabloyu çek, 500 ms içinde motor durmalı)

# ROS2 tarafı:
pytest src/astro_base/test/  # Unit testler
ros2 topic hz /imu/data_raw  # 50 Hz kontrol
ros2 topic echo /arduino/diagnostics  # Watchdog flag kontrolü
```

### Dokümantasyon Eksikleri:
- Pin şeması (Fritzing/KiCad).
- Kalibrasyon prosedürü (enkoder CPR, teker çapı ölçümü).
- Troubleshooting kılavuzu (CRC fail, watchdog timeout senaryoları).

---

**İnceleme Tarihi:** 2025-01-20  
**İnceleyici:** Senior ROS2/Embedded Systems Code Reviewer  
**Sonraki İnceleme:** Düzeltmeler sonrası (tahmini 1 hafta)


## AŞAMA 3 — Gemini (Test/QA Uzmanı): Test & QA Analizi

*Model: `LLMName.GEMINI_2_5_PRO`*

Harika bir görev tanımı ve son derece detaylı bir kod inceleme raporu. Bir Test/QA Uzmanı olarak bu tür bir girdiyle çalışmak, test stratejisini oluşturmayı hem daha kolay hem de daha etkili kılıyor. Proje bağlamını (rosidl monorepo fork, özel buffer paketleri) ve verilen kod/raporu dikkate alarak istenen analizleri aşağıda sunuyorum.

---

### **Genel QA Değerlendirmesi**

Proje, sağlam bir temel üzerine kurulmuş ancak kod inceleme raporunda belirtildiği gibi **üretim kalitesine ulaşması için kritik ve önemli test döngülerinden geçmesi gereken** bir prototip aşamasındadır. QA stratejimiz, bu rapordaki bulguları bir "doğrulama listesi" olarak kullanmak ve bu bulguların ötesindeki "bilinmeyenleri" ortaya çıkarmak üzerine kurulmalıdır. Özellikle donanım etkileşimleri, zamanlama sorunları ve veri bütünlüğü en yüksek riskli alanlardır.

---

### **1. Önerilen Test Senaryoları**

Testleri, kapsamlarına göre Birim, Entegrasyon ve Sistem/Dayanıklılık olarak üç ana kategoriye ayırıyorum.

#### **A. Birim Testleri (Unit Tests)**

Bu testler, tek bir fonksiyonun veya sınıfın harici bağımlılıklar olmadan (mümkünse) doğru çalıştığını doğrular. Hızlı çalışırlar ve CI/CD süreçlerinin temelini oluştururlar.

*   **`protocol.h` / `serial_bridge.py` - CRC-8 Fonksiyonu Testi:**
    *   **Senaryo 1.1 (Pozitif):** Python ve C++ `crc8` fonksiyonlarına bilinen bir veri bloğu ve beklenen CRC değeri ile test vektörleri sağlanır. Sonuçların eşleştiği doğrulanır. (Örn: `[0x09, 0x02, 0x00, 0x00, 0x80, 0x41, 0x00, 0x00, 0x00, 0x42]` verisi için CRC'nin ne olması gerektiği hesaplanıp test edilir).
    *   **Senaryo 1.2 (Negatif):** Boş veri
